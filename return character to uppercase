#include <stdio.h>
#include <ctype.h>

char to_upper(char c);

int main(void) {
    char c;
    scanf(" %c", &c);
    
    printf("Please enter a letter: %c", to_upper(c));
}

char to_upper(char c) {
    
    if ( c >= 'a' && c <= 'z' ) return toupper(c);
    else return 0;
}
